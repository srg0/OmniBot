# Narrow transitive deps so installs resolve quickly (pip backtracks hard on these otherwise)
pyasn1>=0.6.1,<0.7
pyasn1-modules>=0.4.1,<0.5
regex>=2024.11.6
referencing>=0.36.0
rpds-py>=0.29.0
numpy>=2.4.0,<3
jsonschema>=4.23.0,<5
propcache>=0.3.0

requests
python-dotenv
# fastembed (semantic-router[fastembed]) requires Pillow<11 on Python 3.12
Pillow>=10.3.0,<11.0.0
fastapi
uvicorn[standard]
google-genai>=1.50.0
bleak
python-multipart
opencv-python-headless
imageio
imageio-ffmpeg
# fastembed is installed via this extra; keep bounds aligned (0.1.x pins fastembed<0.4 on Python <3.13)
semantic-router[fastembed]>=0.1.12